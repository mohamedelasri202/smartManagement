services:
  mongodb:
    image: 'mongo:latest'
    container_name: logistics-mongodb
    environment:
      - 'MONGO_INITDB_DATABASE=logistics_db'
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: